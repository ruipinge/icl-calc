# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# Cloudflare Direct Upload staging tree, assembled by the preview job
# (#121). Ephemeral in CI; ignored so a local dry run cannot be committed.
/upload

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# private working files — never publish (own inner git repo)
docs/PRIVATE/
