node_modules/
test-results/
playwright-report/
.serve/
.serve-subject/

# Output of the `visual` capture-tool project (histogram-visual.spec.ts).
# Regenerated on demand; never committed.
histogram-shots/
